import { ThemeSettings } from "../../configs/types";

export const defaultSettings: ThemeSettings = {
  themeMode: 'light',
  themeContrast: 'default',
  themeLayout: 'vertical',
  themeColor: 'default',
}
